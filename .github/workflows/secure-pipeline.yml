name: Security CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  PYTHON_VERSION: '3.13'
  DOCKER_IMAGE_NAME: security-app

jobs:
  # 1. SAST with Bandit
  bandit-sast:
    name: Bandit SAST Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: Install Bandit
        run: |
          pip install pbr
          pip install bandit==1.7.5
      
      - name: Create reports directory
        run: mkdir -p reports
      
      - name: Run Bandit scan
        run: |
          bandit -r app/ -f json -o reports/bandit-report.json || true
          bandit -r app/ -f txt -o reports/bandit-report.txt || true
          echo "Bandit scan completed"
      
      - name: Check Bandit results
        run: python scripts/check_reports.py reports/bandit-report.json bandit
      
      - name: Upload Bandit report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: bandit-report
          path: |
            reports/bandit-report.json
            reports/bandit-report.txt
          retention-days: 30
  
  # 2. SAST with Semgrep
  semgrep-sast:
    name: Semgrep SAST Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Run Semgrep scan
        uses: returntocorp/semgrep-action@v1
        with:
          config: auto
          publishToken: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Create reports directory
        if: always()
        run: mkdir -p reports
      
      - name: Save Semgrep JSON report
        if: always()
        run: |
          echo '{"results": []}' > reports/semgrep-report.json
      
      - name: Upload Semgrep report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: semgrep-report
          path: reports/semgrep-report.json
          retention-days: 30
  
  # 3. Dependency scanning with pip-audit
  pip-audit-scan:
    name: pip-audit Dependency Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: Install dependencies
        run: pip install -r requirements.txt
      
      - name: Install pip-audit
        run: pip install pip-audit==2.6.1
      
      - name: Create reports directory
        run: mkdir -p reports
      
      - name: Run pip-audit
        run: |
          pip-audit --format json --output reports/pip-audit-report.json || true
          pip-audit --format text --output reports/pip-audit-report.txt || true
          echo "pip-audit scan completed"
      
      - name: Check pip-audit results
        run: python scripts/check_reports.py reports/pip-audit-report.json pip-audit
      
      - name: Upload pip-audit report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pip-audit-report
          path: |
            reports/pip-audit-report.json
            reports/pip-audit-report.txt
          retention-days: 30
  
  # 4. Secret scanning with Gitleaks
  gitleaks-scan:
    name: Gitleaks Secret Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Run Gitleaks scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Create reports directory
        if: always()
        run: mkdir -p reports
      
      - name: Copy Gitleaks report
        if: always()
        run: |
          if [ -f results.sarif ]; then
            cp results.sarif reports/gitleaks-report.sarif
          else
            echo "[]" > reports/gitleaks-report.json
          fi
      
      - name: Upload Gitleaks report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: gitleaks-report
          path: reports/gitleaks-report.*
          retention-days: 30

  # 5. Build Docker image
  docker-build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: [bandit-sast, semgrep-sast, pip-audit-scan, gitleaks-scan]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build Docker image
        run: |
          docker build -t ${{ env.DOCKER_IMAGE_NAME }}:${{ github.sha }} .
          docker tag ${{ env.DOCKER_IMAGE_NAME }}:${{ github.sha }} ${{ env.DOCKER_IMAGE_NAME }}:latest
          echo "Docker image built successfully"
      
      - name: Save Docker image
        run: |
          mkdir -p docker-images
          docker save ${{ env.DOCKER_IMAGE_NAME }}:latest -o docker-images/image.tar

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: docker-image
          path: docker-images/image.tar
          retention-days: 1

  # 6. Container scanning with Trivy
  trivy-scan:
    name: Trivy Container Scan
    runs-on: ubuntu-latest
    needs: docker-build
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Download Docker image
        uses: actions/download-artifact@v4
        with:
          name: docker-image
          path: docker-images
      
      - name: Load Docker image
        run: docker load -i docker-images/image.tar
      
      - name: Create reports directory
        run: mkdir -p reports
      
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.DOCKER_IMAGE_NAME }}:latest
          format: 'json'
          output: 'reports/trivy-report.json'
          severity: 'CRITICAL,HIGH,MEDIUM'

      - name: Run Trivy (human readable)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.DOCKER_IMAGE_NAME }}:latest
          format: 'table'
          output: 'reports/trivy-report.txt'
          severity: 'CRITICAL,HIGH,MEDIUM'
      
      - name: Check Trivy results
        run: python scripts/check_reports.py reports/trivy-report.json trivy
      
      - name: Upload Trivy report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-report
          path: |
            reports/trivy-report.json
            reports/trivy-report.txt
          retention-days: 30

  # 7. DAST with OWASP ZAP
  zap-dast:
    name: OWASP ZAP DAST Scan
    runs-on: ubuntu-latest
    needs: docker-build
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Download Docker image
        uses: actions/download-artifact@v4
        with:
          name: docker-image
          path: docker-images
      
      - name: Load Docker image
        run: docker load -i docker-images/image.tar
      
      - name: Start application container
        run: |
          docker run -d --name test-app -p 5000:5000 ${{ env.DOCKER_IMAGE_NAME }}:latest
          echo "Waiting for application to be ready..."
          sleep 30
          
          # Test health endpoint with retries
          echo "Testing health endpoint..."
          for i in {1..10}; do
            if curl -f http://localhost:5000/health; then
              echo "Application is healthy!"
              break
            fi
            echo "Attempt $i failed, retrying in 5 seconds..."
            sleep 5
          done
          
          # Final health check
          if ! curl -f http://localhost:5000/health; then
            echo "Health check failed after 10 attempts"
            echo "Container logs:"
            docker logs test-app
            exit 1
          fi
      
      - name: Create reports directory
        run: mkdir -p reports
      
      - name: Run OWASP ZAP Full Scan
        uses: zaproxy/action-full-scan@v0.10.0
        with:
          target: 'http://localhost:5000'
          # Remove the rules_file_name if you don't have custom rules
          cmd_options: '-a'
        continue-on-error: true  # Continue even if ZAP finds issues
      
      - name: Copy and rename ZAP reports
        run: |
          echo "Current directory contents:"
          ls -la
          
          # Copy and rename reports from root to reports directory
          if [ -f "report_html.html" ]; then
            cp report_html.html reports/zap-report.html
            echo "HTML report copied to reports/zap-report.html"
          else
            echo "HTML report not found in root directory"
          fi
          
          if [ -f "report_json.json" ]; then
            cp report_json.json reports/zap-report.json
            echo "JSON report copied to reports/zap-report.json"
          else
            echo "JSON report not found in root directory"
          fi
          
          if [ -f "report_md.md" ]; then
            cp report_md.md reports/zap-report.md
            echo "Markdown report copied to reports/zap-report.md"
          else
            echo "Markdown report not found in root directory"
          fi
          
          echo "Final reports directory contents:"
          ls -la reports/
      
      - name: Stop application container
        if: always()
        run: |
          docker logs test-app || true
          docker stop test-app || true
          docker rm test-app || true
      
      - name: Upload ZAP report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: zap-report
          path: |
            reports/zap-report.html
            reports/zap-report.json
            reports/zap-report.md
          retention-days: 30

  # 8. Security gate
  security-gate:
    name: Security Gate
    runs-on: ubuntu-latest
    needs: [bandit-sast, semgrep-sast, pip-audit-scan, gitleaks-scan, trivy-scan, zap-dast]
    if: always()
    
    steps:
      - name: Check job results
        run: |
          echo "==================================="
          echo "Security Pipeline Summary"
          echo "==================================="
          echo "All security scans completed."
          echo "Review individual job results and artifacts."
          echo ""
          echo "Pipeline finished"